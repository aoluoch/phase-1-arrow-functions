// Define the function expression for divide
const divide = function(a, b) {
    return a / b;
  };
  
  // Define the arrow function for square
  const square = (x) => {
    return x * x;
  };
  
  // Define the arrow function for add
  const add = (a, b) => {
    return a + b;
  };


  